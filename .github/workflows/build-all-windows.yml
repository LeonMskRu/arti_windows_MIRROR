name: Build Arti + Transports (Windows x64)

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"  # ежедневно 06:00 UTC

permissions:
  contents: write

env:
  ARTI_MIRROR_REPO: arti-mirror   # имя зеркала — меняйте здесь
  LYREBIRD_REF:     main          # можно заменить на конкретный тег/коммит
  SNOWFLAKE_REF:    main
  WEBTUNNEL_REF:    main
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true  # Node.js 24 opt-in (обязателен с 02.06.2026)

jobs:
  build-release:
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh

    steps:
      # ──────────────────────────────────────────
      # Checkout meta repo
      # ──────────────────────────────────────────
      - name: Checkout meta repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      # ──────────────────────────────────────────
      # Определяем последнюю версию через git ls-remote
      # без клонирования репозитория целиком.
      #
      # "git clone --depth 1" получает только HEAD-коммит и теги,
      # которые непосредственно на него указывают. В mirror-репо
      # теги arti-v* указывают на исторические коммиты → они
      # невидимы в shallow clone. git ls-remote не клонирует репо,
      # а запрашивает только список refs.
      #
      # Формат строк ls-remote:
      #   <sha>\trefs/tags/arti-v2.3.0
      #   <sha>\trefs/tags/arti-v2.3.0^{}   <- peeled annotated tag (исключаем)
      # ──────────────────────────────────────────
      - name: Detect latest Arti version
        id: arti_ver
        run: |
          $mirrorUrl = "https://github.com/${{ github.repository_owner }}/${{ env.ARTI_MIRROR_REPO }}"
          Write-Host "🔍 Querying tags from: $mirrorUrl"

          $raw = git ls-remote --tags --sort=-v:refname $mirrorUrl "refs/tags/arti-*" 2>&1
          if ($LASTEXITCODE -ne 0) {
            Write-Error "❌ git ls-remote failed (exit $LASTEXITCODE)"
            exit 1
          }

          $tags = $raw |
                  Where-Object { $_ -notmatch '\^\{\}\s*$' } |
                  ForEach-Object { ($_ -split '\s+')[-1] -replace '^refs/tags/', '' }

          Write-Host "=== Последние теги Arti ==="
          $tags | Select-Object -First 5 | ForEach-Object { Write-Host "  $_" }

          $ver = $tags | Select-Object -First 1
          if ([string]::IsNullOrEmpty($ver)) {
            Write-Error "❌ No arti-* tags found in mirror. Cannot proceed."
            exit 1
          }

          Write-Host "✅ Latest Arti version: $ver"
          "arti_version=$ver" | Out-File -Append -FilePath $env:GITHUB_ENV

      # ──────────────────────────────────────────
      # Пропуск если релиз уже существует.
      #
      # gh release view возвращает exit code 1 если релиза нет.
      # Без явного "exit 0" — $LASTEXITCODE == 1 остаётся
      # активным, pwsh выходит с кодом 1, GHA помечает шаг
      # как FAILED, все последующие шаги пропускаются.
      # ──────────────────────────────────────────
      - name: Check if release already exists
        id: check_release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          $ver = $env:arti_version
          $null = gh release view $ver 2>&1
          if ($LASTEXITCODE -eq 0) {
            Write-Host "⏭️ Release $ver already exists — skipping build."
            "skip=true"  | Out-File -Append $env:GITHUB_OUTPUT
          } else {
            Write-Host "🆕 Release $ver not found — proceeding with build."
            "skip=false" | Out-File -Append $env:GITHUB_OUTPUT
          }
          exit 0

      # ──────────────────────────────────────────
      # Клонируем mirror точечно по тегу.
      # --depth 1 --branch <tag> безопасен: клонирует ровно тот
      # коммит, на который указывает тег, без лишней истории.
      # ──────────────────────────────────────────
      - name: Clone Arti mirror at tag
        if: steps.check_release.outputs.skip == 'false'
        run: |
          $ver = $env:arti_version
          $mirrorUrl = "https://github.com/${{ github.repository_owner }}/${{ env.ARTI_MIRROR_REPO }}"
          Write-Host "📥 Cloning $mirrorUrl at tag $ver ..."
          git clone --depth 1 --branch $ver $mirrorUrl arti
          if ($LASTEXITCODE -ne 0) {
            Write-Error "❌ git clone failed (exit $LASTEXITCODE)"
            exit 1
          }
          Write-Host "✅ Cloned Arti at $ver"

      # ──────────────────────────────────────────
      # Rust toolchain
      # ──────────────────────────────────────────
      - name: Setup Rust (MSVC x64)
        if: steps.check_release.outputs.skip == 'false'
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      # ──────────────────────────────────────────
      # Cargo cache
      # ──────────────────────────────────────────
      - name: Cache Cargo registry + build
        if: steps.check_release.outputs.skip == 'false'
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            arti/target
          key: cargo-win-x64-${{ env.arti_version }}
          restore-keys: |
            cargo-win-x64-

      # ──────────────────────────────────────────
      # SQLite static lib через vcpkg.
      # $env:VCPKG_INSTALLATION_ROOT — стандартная переменная
      # GHA windows runner (C:\vcpkg).
      # ──────────────────────────────────────────
      - name: Install SQLite static lib
        if: steps.check_release.outputs.skip == 'false'
        run: |
          $vcpkgRoot = $env:VCPKG_INSTALLATION_ROOT
          if ([string]::IsNullOrEmpty($vcpkgRoot)) { $vcpkgRoot = "C:\vcpkg" }
          Write-Host "ℹ️ vcpkg root: $vcpkgRoot"

          & "$vcpkgRoot\vcpkg.exe" update 2>&1 | Write-Host
          & "$vcpkgRoot\vcpkg.exe" install sqlite3:x64-windows-static
          if ($LASTEXITCODE -ne 0) {
            Write-Error "❌ vcpkg install sqlite3 failed (exit $LASTEXITCODE)"
            exit 1
          }

          $libDir = "$vcpkgRoot\installed\x64-windows-static\lib"
          if (-not (Test-Path $libDir)) {
            Write-Error "❌ Expected SQLITE3_LIB_DIR not found: $libDir"
            exit 1
          }

          "SQLITE3_LIB_DIR=$libDir" | Out-File -Append $env:GITHUB_ENV
          "SQLITE3_STATIC=1"        | Out-File -Append $env:GITHUB_ENV
          Write-Host "✅ SQLITE3_LIB_DIR=$libDir"

      # ──────────────────────────────────────────
      # Build Arti x64
      # ──────────────────────────────────────────
      - name: Build arti.exe (x64)
        if: steps.check_release.outputs.skip == 'false'
        working-directory: arti
        run: |
          cargo build --release `
            --target x86_64-pc-windows-msvc `
            --package arti `
            --bin arti `
            --features static

      - name: Copy arti binary
        if: steps.check_release.outputs.skip == 'false'
        run: |
          New-Item -ItemType Directory -Force dist | Out-Null
          Copy-Item arti/target/x86_64-pc-windows-msvc/release/arti.exe `
                    dist/arti-x64.exe -Force
          Write-Host "✅ dist/arti-x64.exe ready"

      # ──────────────────────────────────────────
      # Go toolchain (транспорты)
      # ──────────────────────────────────────────
      - name: Setup Go
        if: steps.check_release.outputs.skip == 'false'
        uses: actions/setup-go@v5
        with:
          go-version: stable
          cache: false   # go.sum нет в корне мета-репо — отключаем автокэш

      # ──────────────────────────────────────────
      # Lyrebird
      # ──────────────────────────────────────────
      - name: Clone lyrebird (${{ env.LYREBIRD_REF }})
        if: steps.check_release.outputs.skip == 'false'
        run: |
          git clone --depth 1 --branch ${{ env.LYREBIRD_REF }} `
            https://gitlab.torproject.org/tpo/anti-censorship/pluggable-transports/lyrebird.git

      - name: Build lyrebird x64
        if: steps.check_release.outputs.skip == 'false'
        working-directory: lyrebird/cmd/lyrebird
        run: go build -o ../../../dist/lyrebird-x64.exe .

      # ──────────────────────────────────────────
      # Snowflake
      # ──────────────────────────────────────────
      - name: Clone snowflake (${{ env.SNOWFLAKE_REF }})
        if: steps.check_release.outputs.skip == 'false'
        run: |
          git clone --depth 1 --branch ${{ env.SNOWFLAKE_REF }} `
            https://gitlab.torproject.org/tpo/anti-censorship/pluggable-transports/snowflake.git

      - name: Build snowflake-client x64
        if: steps.check_release.outputs.skip == 'false'
        working-directory: snowflake/client
        run: go build -o ../../dist/snowflake-client-x64.exe .

      # ──────────────────────────────────────────
      # WebTunnel
      # ──────────────────────────────────────────
      - name: Clone webtunnel (${{ env.WEBTUNNEL_REF }})
        if: steps.check_release.outputs.skip == 'false'
        run: |
          git clone --depth 1 --branch ${{ env.WEBTUNNEL_REF }} `
            https://gitlab.torproject.org/tpo/anti-censorship/pluggable-transports/webtunnel.git

      - name: Build webtunnel-client x64
        if: steps.check_release.outputs.skip == 'false'
        working-directory: webtunnel/main/client
        run: go build -o ../../../dist/webtunnel-client-x64.exe .

      # ──────────────────────────────────────────
      # Packaging: отдельные ZIP-ы
      # ──────────────────────────────────────────
      - name: Create ZIPs
        if: steps.check_release.outputs.skip == 'false'
        run: |
          if (-not (Test-Path dist/arti-x64.exe)) {
            Write-Error "❌ dist/arti-x64.exe not found — build failed"
            exit 1
          }
          Compress-Archive -Path dist/arti-x64.exe             -DestinationPath dist/arti-x64.zip             -Force
          Compress-Archive -Path dist/lyrebird-x64.exe         -DestinationPath dist/lyrebird-x64.zip          -Force
          Compress-Archive -Path dist/snowflake-client-x64.exe -DestinationPath dist/snowflake-x64.zip         -Force
          Compress-Archive -Path dist/webtunnel-client-x64.exe -DestinationPath dist/webtunnel-x64.zip         -Force
          Compress-Archive -Path dist/*.exe                    -DestinationPath dist/bundle-executables.zip    -Force
          Write-Host "✅ All ZIPs created"

      # ──────────────────────────────────────────
      # SHA256 — только для .zip файлов
      # Два пробела между хешем и именем — стандарт sha256sum
      # ──────────────────────────────────────────
      - name: SHA256 checksums
        if: steps.check_release.outputs.skip == 'false'
        run: |
          $zips = Get-ChildItem dist -Filter *.zip
          if ($zips.Count -gt 0) {
            $zips | ForEach-Object {
              $h = Get-FileHash $_.FullName -Algorithm SHA256
              "$($h.Hash)  $($_.Name)"
            } | Out-File dist/SHA256SUMS.txt -Encoding ascii
            Write-Host "✅ SHA256SUMS.txt written for $($zips.Count) zip(s)"
          } else {
            Write-Error "❌ No ZIP files found for checksum"
            exit 1
          }

      # ──────────────────────────────────────────
      # Release notes
      # ──────────────────────────────────────────
      - name: Prepare release notes
        if: steps.check_release.outputs.skip == 'false'
        run: |
          @"
          ## Tor / Arti Windows x64 Bundle

          | Binary | Source |
          |--------|--------|
          | **arti-x64** | Arti ${{ env.arti_version }} — stable (``--features static``) |
          | **lyrebird-x64** | lyrebird ``${{ env.LYREBIRD_REF }}`` |
          | **snowflake-client-x64** | snowflake ``${{ env.SNOWFLAKE_REF }}`` |
          | **webtunnel-client-x64** | webtunnel ``${{ env.WEBTUNNEL_REF }}`` |

          Verify downloads with ``SHA256SUMS.txt``.
          "@ | Out-File RELEASE_NOTES.md -Encoding utf8

      # ──────────────────────────────────────────
      # GitHub Release
      # ──────────────────────────────────────────
      - name: Create GitHub Release
        if: steps.check_release.outputs.skip == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.arti_version }}
          name: "Tor bundle (${{ env.arti_version }})"
          body_path: RELEASE_NOTES.md
          fail_on_unmatched_files: false
          files: |
            dist/arti-x64.zip
            dist/lyrebird-x64.zip
            dist/snowflake-x64.zip
            dist/webtunnel-x64.zip
            dist/bundle-executables.zip
            dist/SHA256SUMS.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
